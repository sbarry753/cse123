# Project Name
TARGET = capstone_pedal

# Sources
# Add your .cpp files here! (Do NOT add .h files here)
CPP_SOURCES = main.cpp effect.cpp

# Library Locations
# This assumes your project is inside a subfolder of the DaisyExamples repo
# If not, you may need to provide the full path to your libDaisy and DaisySP folders
LIBDAISY_DIR = ./libDaisy
DAISYSP_DIR = ./DaisySP

# Core Makefile Includes
# This is the "magic" that handles the ARM compiler settings for the Seed
include $(LIBDAISY_DIR)/core/Makefile
include $(DAISYSP_DIR)/Makefile
